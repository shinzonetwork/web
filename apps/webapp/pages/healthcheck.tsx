// apps/webapp/pages/healthcheck.tsx

export default function Healthcheck() {
  // This route exists only to force Next to emit pages-manifest.json
  // It won't interfere with your / (root) app route.
  return null;
}
