# # Hoist wagmi packages to the workspace root so editors / tools that resolve from
# # the repo root can still find typings and entrypoints after `pnpm install`.
# public-hoist-pattern[]=wagmi
# public-hoist-pattern[]=@wagmi/*
